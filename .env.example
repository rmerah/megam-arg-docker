# MEGAM ARG Detection — Configuration Docker
# Copiez ce fichier en .env et modifiez les valeurs selon vos besoins

# Port d'accès à l'application (défaut: 8080)
MEGAM_PORT=8080

# Nombre de threads pour le pipeline (défaut: 4)
MEGAM_THREADS=4
